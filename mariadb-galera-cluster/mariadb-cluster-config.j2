[mysqld]
binlog_format=ROW
default-storage-engine=innodb
innodb_autoinc_lock_mode=2
bind-address=0.0.0.0

# MariaDB Galera Cluster Configuration
wsrep_on=ON
wsrep_provider=/usr/lib/galera/libgalera_smm.so
wsrep_cluster_name="{{ mariadb_cluster_name }}"
wsrep_cluster_address="gcomm://{{ groups['mariadb_cluster'] | map('extract', hostvars, ['ansible_default_ipv4', 'address']) | join(',') }}"
wsrep_sst_method=mariabackup
wsrep_node_address="{{ ansible_default_ipv4.address }}"
wsrep_node_name="{{ inventory_hostname }}"

# MariaDB Galera Strict Mode
wsrep_provider_options="base_port=4567; gmcast.segment=0; pc.announce_timeout=15.0; evs.keepalive_period=PT3S; evs.suspect_timeout=PT1M30S; evs.inactive_timeout=PT3M; evs.install_timeout=PT1M; evs.send_window=4; evs.user_send_window=2"

# InnoDB Performance Tuning
innodb_buffer_pool_size=1G
innodb_log_file_size=256M
innodb_flush_log_at_trx_commit=1
innodb_flush_method=O_DIRECT
innodb_doublewrite=ON
innodb_file_per_table=1
innodb_autoinc_lock_mode=2

# Connection & Thread Pool
max_connections=500
thread_cache_size=128

# Character Set
character-set-server=utf8mb4
collation-server=utf8mb4_unicode_ci

[client]
default-character-set=utf8mb4

[mysql]
default-character-set=utf8mb4